services:
  caddy:
    container_name: gateway_caddy
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - caddy_data:/data
      - caddy_config:/config
    network_mode: host
    restart: unless-stopped

volumes:
  caddy_data:
  caddy_config: