{
  "apps": {
    "layer4": {
      "servers": {
        "relay_443": {
          "listen": [":443"],
          "routes": [
            {
              "handle": [
                {
                  "handler": "proxy",
                  "upstreams": [{"dial": ["tcp/${DEPLOY_HOST}:443"]}]
                }
              ]
            }
          ]
        },
        "relay_vless": {
          "listen": [":${RELAY_VLESS_PORT}"],
          "routes": [
            {
              "handle": [
                {
                  "handler": "proxy",
                  "upstreams": [{"dial": ["tcp/${DEPLOY_HOST}:${RELAY_VLESS_PORT}"]}]
                }
              ]
            }
          ]
        },
        "relay_xhttp": {
          "listen": [":${RELAY_XHTTP_PORT}"],
          "routes": [
            {
              "handle": [
                {
                  "handler": "proxy",
                  "upstreams": [{"dial": ["tcp/${DEPLOY_HOST}:${RELAY_XHTTP_PORT}"]}]
                }
              ]
            }
          ]
        },
        "relay_grpc": {
          "listen": [":${RELAY_GRPC_PORT}"],
          "routes": [
            {
              "handle": [
                {
                  "handler": "proxy",
                  "upstreams": [{"dial": ["tcp/${DEPLOY_HOST}:${RELAY_GRPC_PORT}"]}]
                }
              ]
            }
          ]
        },
        "relay_awg_udp": {
          "listen": ["udp/:${RELAY_AWG_PORT}"],
          "routes": [
            {
              "handle": [
                {
                  "handler": "proxy",
                  "upstreams": [{"dial": ["udp/${DEPLOY_HOST}:${RELAY_AWG_PORT}"]}]
                }
              ]
            }
          ]
        },
        "relay_ws": {
          "listen": [":${RELAY_WS_PORT}"],
          "routes": [
            {
              "handle": [
                {
                  "handler": "proxy",
                  "upstreams": [{"dial": ["tcp/${DEPLOY_HOST}:${RELAY_WS_PORT}"]}]
                }
              ]
            }
          ]
        },
        "relay_awg_udp": {
          "listen": ["udp/:${RELAY_AWG_PORT}"],
          "routes": [
            {
              "handle": [
                {
                  "handler": "proxy",
                  "upstreams": [{"dial": ["udp/${DEPLOY_HOST}:${RELAY_HYSTERIA_PORT}"]}]
                }
              ]
            }
          ]
        },
         "relay_hysteria": {
          "listen": ["udp/:${RELAY_HYSTERIA_PORT}"],
          "routes": [
            {
              "handle": [
                {
                  "handler": "proxy",
                  "upstreams": [{"dial": ["udp/${DEPLOY_HOST}:${RELAY_HYSTERIA_PORT}"]}]
                }
              ]
            }
          ]
        }
      }
    }
  }
}
