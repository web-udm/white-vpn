services:
  app:
    container_name: bot_franken
    build:
      context: .
      dockerfile: docker/franken/Dockerfile
      network: host
    environment:
      APP_ENV: test
      SERVER_NAME:
    tty: true
