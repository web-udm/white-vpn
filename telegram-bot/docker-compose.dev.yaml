services:
  app:
    container_name: bot_dev_app
    image: ghcr.io/web-udm/white-vpn-bot:dev
    ports:
      - "8081:80"
    env_file: .env.dev
    environment:
      APP_ENV: dev
      SERVER_NAME: ":80"
    volumes:
      - dev_data:/app/var
    restart: unless-stopped

  worker:
    container_name: bot_dev_worker
    image: ghcr.io/web-udm/white-vpn-bot:dev
    env_file: .env.dev
    environment:
      APP_ENV: dev
    command: php bin/console messenger:consume async --time-limit=3600
    volumes:
      - dev_data:/app/var
    restart: unless-stopped
    depends_on:
      - app

volumes:
  dev_data: